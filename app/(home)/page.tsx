import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home",
};

const Start = () => {
  return <h1 className="text-3xl font-bold underline text-red-400">HI</h1>;
};

export default Start;
